import React from "react";
import * as styles from "./commonInfo.module.css"

const CommonInfo = (props) => {
    return(
        <div className={styles.common__wrapper}>
            CommonInfo
        </div>
    )
};

export default CommonInfo;