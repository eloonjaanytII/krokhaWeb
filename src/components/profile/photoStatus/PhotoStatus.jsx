import React from "react";
import * as styles from "./photoStatus.module.css"

const PhotoStatus = (props) => {
    return(
        <div className = {styles.photoStatus__wrapper}>
            PhotoStatus
        </div>
    )
};

export default PhotoStatus;