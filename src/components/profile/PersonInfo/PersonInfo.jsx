import React from "react";
import * as styles from "./personInfo.module.css"

const PersonInfo = (props) => {
    return(
        <div className = {styles.personInfo__wrapper}>
            PersonInfo
        </div>
    )
};

export default PersonInfo;